<div class="wrapper col-md-6 col-md-offset-3 col-sm-10 col-sm-offset-1">
    <p class="left"><strong>アイコン</strong></p>
    <p class="left"><%= profile_img(@user) %></p>
    <p class="left"><strong>名前</strong></p>
    <p class="left"><%= @user.name %></p>
    <p class="left"><strong>メール</strong></p>
    <p class="left"><%= @user.email%></p>
</div>
<div class="wrapper col-md-6 col-md-offset-3 col-sm-10 col-sm-offset-1">
    <p class="left"><strong>フォロー一覧</strong>
      <%= render partial: 'follow_index', locals: { user: @user} %></p>
    <p class="left"><strong>フォロワー一覧</strong>
      <%= render partial: 'follower_index', locals: { user: @user} %></P>
</div>
